spring.application.name=PRATIK-DAIRY-PAYMENT



spring.datasource.driver-class-name = org.mariadb.jdbc.Driver
spring.datasource.url = jdbc:mariadb://localhost:3306/pratik_dairy_payment?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username = root
spring.datasource.password = 123456
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

server.port = 8084

springdoc.swagger-ui.path = /docs

jwt.secret=pratikdairypratikdairypratikdairypratikdairy

spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true

management.endpoints.web.exposure.include=health,info
management.endpoints.health.show-details=never


#Eureka configuration
eureka.instance.prefer-ip-address = true
eureka.client.fetch-registry = true
eureka.client.register-with-eureka = true
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/


# -------- Razorpay --------
# NEVER commit real values here - use environment variables in every real deployment.
# Get these from the Razorpay Dashboard > Settings > API Keys, and > Webhooks (for the
# webhook secret, set when you register https://<your-domain>/payments/webhook/razorpay
# as the webhook URL).
# -------- Razorpay --------
# NEVER commit real values here - use environment variables in every real deployment.
# Get these from the Razorpay Dashboard > Settings > API Keys, and > Webhooks (for the
# webhook secret, set when you register https://<your-domain>/payments/webhook/razorpay
# as the webhook URL).
razorpay.key-id=${RAZORPAY_KEY_ID:rzp_test_SlCAfQpHujynkq}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:pASdjNg7LttbV2vikmK92oFU}
razorpay.webhook-secret=${RAZORPAY_WEBHOOK_SECRET:dummy_webhook_secret}